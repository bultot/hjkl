# hjkl

A context-aware keyboard cheat-sheet for macOS. Hit a hotkey, see the shortcuts
for whatever app you're in (cmux, AeroSpace, …), read straight from each tool's
own config. Native SwiftUI, menu-bar app, read-only.

Status: early WIP. Build: `xcodegen generate && xcodebuild -scheme hjkl build`.
